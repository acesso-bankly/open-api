name: NEW Sync OAS DEV
on:
  push:
    branches:
      - testeRobozinho
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: readmeio/rdme@vXX
        with:
          rdme: openapi './apis/v1/helpers/schema.yaml' --key=JBmFybf81zYjJaqEp83JpXuApq3okY79 --version=v0.05